model:
  model_name: "${CACHECODEC_MODEL_ROOT}/Qwen3-0.6B"
  generation_config:
    do_sample: false
    max_new_tokens: 16
output:
  output_dir: "${CACHECODEC_RUN_ROOT}/results/receiver_only"
eval:
  dataset: mmlu-redux
  local_jsonl_file: "${CACHECODEC_DATA_ROOT}/mmlu_redux_test.jsonl"
  gpu_ids: [1]
  answer_method: generate
  use_cot: false
  use_template: true
  sample_interval: 1
